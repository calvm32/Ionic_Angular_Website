import { AngularConfig } from '@angular/cli';

const config: AngularConfig = {
  appId: 'io.ionic.starter',
  appName: 'Website_V3',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
